apiVersion: v1
kind: Service
metadata:
  name: {{ include "opervn.fullname" . }}
  labels:
    {{- include "opervn.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opervn.selectorLabels" . | nindent 4 }}
